<?php

// autoload.php @generated by Composer
// Ce fichier a été généré par Composer pour gérer l'autoloading des dépendances.

if (PHP_VERSION_ID < 50600) {
    // Si la version de PHP est inférieure à 5.6 (version minimale requise par Composer 2.3.0)
    if (!headers_sent()) {
        // Si les en-têtes HTTP n'ont pas encore été envoyés, on envoie une réponse d'erreur 500
        header('HTTP/1.1 500 Internal Server Error');
    }
    
    // On crée un message d'erreur avec la version actuelle de PHP
    $err = 'Composer 2.3.0 dropped support for autoloading on PHP <5.6 and you are running '.PHP_VERSION.', please upgrade PHP or use Composer 2.2 LTS via "composer self-update --2.2". Aborting.'.PHP_EOL;
    
    // Si l'affichage des erreurs est désactivé dans php.ini, on tente d'afficher le message d'erreur dans la sortie standard
    if (!ini_get('display_errors')) {
        // Si l'exécution se fait en ligne de commande ou en mode débogage PHP (phpdbg), on écrit l'erreur sur la sortie d'erreur
        if (PHP_SAPI === 'cli' || PHP_SAPI === 'phpdbg') {
            fwrite(STDERR, $err);
        } elseif (!headers_sent()) {
            // Sinon, si on est dans un environnement web, on envoie l'erreur dans le corps de la réponse HTTP
            echo $err;
        }
    }
    
    // On déclenche une erreur PHP de type E_USER_ERROR avec le message d'erreur
    trigger_error(
        $err,
        E_USER_ERROR
    );
}

// Inclut le fichier d'autoload généré par Composer
require_once __DIR__ . '/composer/autoload_real.php';

// Retourne l'instance du chargeur d'autoload de Composer pour gérer les dépendances
return ComposerAutoloaderInit2185d2f99bcd56787481d9357a5972d3::getLoader();
